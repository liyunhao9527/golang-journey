# Demos

验证语法和特性的可运行小示例,每个示例一个独立目录,可直接 `go run` 运行。

**对照约定**:每个 demo 目录里,Go 文件和同名 `.ts` 文件成对出现(如 `hello.go` / `hello.ts`),是同一逻辑的两种语言实现,可分别运行对照。

课程正文引用的文件是基准 demo,应始终保持可运行并与正文输出一致。临时试验、故意报错和自由改写放进该课的 `sandbox/` 子目录,避免污染后续复习使用的基准代码。
