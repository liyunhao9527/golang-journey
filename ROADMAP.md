# Go 学习路线

面向已有 JavaScript / TypeScript 经验的开发者，落实 [MISSION.md](MISSION.md) 中的系统学习目标。先建立语言心智模型，再决定 CLI、后端或其他方向。

## 阶段与过关证据

这是教学安排，不是学习完成记录。第 1–6 课已有正文；其余主题待编写，拆分方式随练习反馈调整。

| 阶段 | 内容与开放状态 | 过关证据 |
| --- | --- | --- |
| 1 · 程序与基础语法 | 工具链、变量与类型、控制流；第 1–3 课已开放。 | 从空目录运行程序，独立完成条件判断和循环统计。 |
| 2 · 数据与值的传递 | 数组、slice、map、指针；第 4–6 课已开放。后续补充字符串、rune 与函数。 | 预测复制和共享后的修改结果，区分缺失键与零值，安全处理 nil。 |
| 3 · 类型、组合与错误 | 计划：struct、方法、接口、显式 error、defer、包与 module、表驱动测试、泛型入门。 | 将统计逻辑拆成可测试的包，通过接口替换依赖，并覆盖失败分支。 |
| 4 · 并发与取消 | 计划：goroutine、channel、select、sync、context；对照 Promise 与事件循环。 | 实现有并发上限且可取消的任务，解释关闭责任，通过竞态检测。 |
| 5 · 标准库与工程实践 | 计划：文件与 io、JSON、net/http、配置、日志、构建；按兴趣阅读开源项目。 | 交付可运行的 CLI 或 HTTP 服务，提供边界测试、启动说明与验收命令。 |

## 后续短课顺序

1. 字符串、byte 与 rune：区分字节索引和 Unicode 码点遍历，再完成含中文的统计练习。
2. 函数、多返回值与值传递：用输入输出组织逻辑，结合指针复习复制与共享；随后补闭包和可变参数。
3. struct 与方法：描述数据，比较值接收者和指针接收者；对照 TS 对象与类。
4. 接口与组合：理解隐式实现，再用一个小接口替换依赖；对照 TS 结构类型并说明边界。
5. error 与 defer：返回并处理失败、安排资源释放；对照异常传播，区分常规失败和 panic。
6. 包、module 与测试：拆分代码，写表驱动测试，检查边界；重复出现的容器操作再引入泛型。
7. goroutine 与同步：先等待任务结束，再学习 channel、select、共享状态和竞态。
8. context 与取消：明确任务生命周期、关闭责任与超时；与 Promise / 事件循环作概念对照。
9. 标准库：文件与 io → JSON → HTTP 客户端 → net/http 服务与 httptest。
10. 工程化：配置与日志、构建与依赖管理，最后带着问题阅读 cobra 或 gin 的小段核心代码。

每项可以拆成多课。新课只使用已教知识，或在首次出现的位置补充解释；并发学习先用最小任务，文件与 HTTP 任务留到对应标准库课程。

## 项目验收

三个方向均为计划，按兴趣选择；不把完成全部项目设为学习前提。每个新概念继续提供 TS 对照，项目关键实现由学习者完成。

### 目录统计 CLI

遍历目录，按扩展名统计文件数量与大小，支持忽略目录和 JSON 输出。

阶段 5 学完文件 I/O 后开始。验收：空目录、无效路径与权限错误测试，退出码和构建说明。

### 学习书签 API

用 net/http 实现书签增删改查、JSON 校验和统一错误响应，再加入持久化。

阶段 5 学完 HTTP 后开始。验收：httptest 接口测试、非法输入、重启后数据保留和 curl 示例。

### 站点可用性检查器

并发检查一组 URL，限制并发数，支持超时与取消，汇总每个地址的结果。

阶段 4 与 HTTP 客户端基础完成后开始。验收：本地测试服务器覆盖成功、失败、超时与取消，通过竞态检测。

里程碑：先交付最小可运行版本，再加入失败处理和测试，最后补使用说明。CLI 用临时目录验收；API 用临时数据目录或测试数据库验收；并发检查器使用本地 HTTP 测试服务器，避免依赖外站可用性。持久化先用已学文件与 JSON，SQLite 作为选做扩展。

## 每课与复习

读一个概念 → 看 Go / TS 对照 → 预测结果 → 自己修改并运行 → 解释实际结果。练习给出成功条件、参考反馈和官方资料。

下次课先不看答案回忆上一课的问题；阶段练习混入旧知识。能独立写出代码并解释边界后再推进。已开放课程和个人掌握状态分开，后者以 learning-records/ 中的练习证据为准。

## 资料依据

- [A Tour of Go](https://go.dev/tour/)：基础语法、方法、接口和并发主题。
- [Go 语言规范](https://go.dev/ref/spec)：语言规则核对入口。
- [标准库文档](https://pkg.go.dev/std)：文件、JSON、HTTP 与测试 API。
- [现有资料清单](RESOURCES.md)：教学时按具体课题核实来源和版本。
